interface Props {
  brandName: string;
  platform: string;
  tone: string;
  targetAudience: string;
}

export async function ContentGenerate({
  brandName,
  platform,
  tone,
  targetAudience,
}: Props) {
  return;
}
